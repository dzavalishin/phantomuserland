
all: $(TARGET)

prepare::

CLEAN_FILES += $(TARGET)


include $(BUILD_ROOT)/makerules

#	$(RANLIB) $@

ARFLAGS = crs

$(TARGET): $(filter-out $(EXCLUDED_OBJFILES), $(OBJFILES) )
	$(AR) $(ARFLAGS)  $@ $?
	cp $@ $(BUILD_ROOT)/lib




