#CFLAGS += -fstack-check -mpush-args

#PLIBDIR=$(realpath $(PHANTOM_HOME))/phantom/lib
# $(PLIBDIR)/libphantom_c.a

test: main.o ../spinlock.o ../stack.o 
	gcc -g  -o $@ $^ 

main.o: main.c
	gcc -c -o main.o main.c
